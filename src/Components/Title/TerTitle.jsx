import React from "react";

const TerTitle = ({ terTitle }) => {
  return (
    <h4 className="uppercase text-[#429200] font-semibold text-[16px] md:text-lg xl:text-xl tracking-wide">
      {terTitle}
    </h4>
  );
};

export default TerTitle;
